› patasmonkeyのオドメトリ推定の誤差が大きく、困っています。まずは原因究明から進めたいです。
  使ったスクリプトはpm_bringupパッケージにあるpm_bag_global_localization.launch.pyです。
  bagファイルは/bagディレクトリにあるrosbag2_2026_07_26-09_17_38を使って調査するものとします。
 
  ホイールエンコーダ、IMU、VIOの3⃣つをFusionしてlocal ekfによってオドメトリを推定しているので、まずはekfに入力する前にどのようなオドメトリ推定がそれぞれのソースから為されたのか確認したいです。
 
  １．ホイールオドメトリのみによるオドメトリ推定
  ２．ホイールオドメトリによるx方向並進、IMUによるz方向旋回　のみによる推定
  ３．VIOによるオドメトリ推定
 
  の３パターンを比較したいです。
  これらについて解析スクリプトと、実際にオドメトリ推定結果をプロットして可視化してください。
  要領は後述しますが、もっと適している、開発現場で一般的に主流なやり方があるのであれば、その旨を述べて許可を私に求めてください。
 
  （作業要領）
  関係スクリプトはpm_evaluationパッケージの中で作成するのがいいのではないかと思っていますが、もっと適切なやり方があれば教えてください。
  解析結果はbagディレクトリにも結構入っていますが、今回はbagファイルをそのまま可視化するというよりは改善方向性を探るための作業になるので、解析結果もpm_evaluationディレクトリ内のどこかがいいのではないかと思って
  います。
  pm_bag_global_localization.launch.pyを参考にして、local_ekfのconfigファイルを参照し、１〜３のオドメトリ推定をするためのconfigファイルを別途作成してください。実際にオドメトリ推定をするためのスクリプトやlaunch
  ファイルも適宜作成してください。
  プロットする際はGNSS情報を基に走っていたエリアのopen street map等の地図画像にオーバーレイしてほしいので、pm_evaluation/pm_evaluation/cli/plot_bag_trajectories.pyを参考にしてオーバーレイも実装してください。
 
  作成したスクリプトのビルド、実行まで行って、解析結果ファイルを可視化してもらうか、閲覧可能な格納先ディレクトリを示してください。
