User prompt: 今後実装してもらう各種コードにはコメントを丁寧めにつけてほしいです。
その旨をAGENTS.mdに追記してもらえませんか？その上でAGENTS.mdを再度見直して、ここまでの作業に鑑みると表現が冗長と思われる表現を圧縮して全体をコンパクトに保ってください。

Codex final response: 更新しました。[AGENTS.md](../../AGENTS.md) を93行から63行へ整理し、内容を圧縮しました。

追加したコメント方針では、今後の新規・大きく変更するコードについて、特に以下を記録するよう明文化しています。

- 非自明な意図、座標系・単位、アルゴリズムの仮定
- parameter・閾値の意味
- データ所有・コピーやリアルタイム性能上の選択
- 安全に関わる挙動
- APIや処理段階を説明するdocstring

一方で、構文を言い換えるだけのコメントは避け、実装変更時にはコメントも必ず同期させます。

併せて、既存のFoxy build手順、ROS interface互換性、Jetson・車両安全、外部workspace編集、セッション記録のルールを保ったまま統合しました。セッション記録の拡張子も、先ほどの方針に合わせて`.md`へ修正済みです。設定文書のみの変更のためbuildは不要で、`git diff --check`は通過しています。
